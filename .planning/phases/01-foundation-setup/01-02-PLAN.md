---
phase: 01-foundation-setup
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - skills/vault-init/templates/project-dashboard.md
  - skills/vault-init/templates/task-list.md
  - skills/vault-init/templates/inbox-note.md
autonomous: false
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04
must_haves:
  truths:
    - "User can run /vault:init on an empty directory and get a complete PARA folder hierarchy with templates and vault:config.md"
    - "User can run /vault:init on an existing vault and see a non-destructive proposal before any changes"
    - "Three note templates (project dashboard, task list, inbox note) exist as bundled skill assets"
    - "vault:config.md contains folder conventions, frontmatter spec, linking rules, and agent instructions"
  artifacts:
    - path: "skills/vault-init/templates/project-dashboard.md"
      provides: "Project dashboard note template with frontmatter and sections"
      contains: "type: project"
    - path: "skills/vault-init/templates/task-list.md"
      provides: "Task list note template with active/completed/blocked sections"
      contains: "type: task-list"
    - path: "skills/vault-init/templates/inbox-note.md"
      provides: "Inbox note template with minimal frontmatter"
      contains: "type: inbox"
  key_links:
    - from: "skills/vault-init/SKILL.md"
      to: "skills/vault-init/templates/*.md"
      via: "CLAUDE_SKILL_DIR reference in skill body"
      pattern: "CLAUDE_SKILL_DIR.*templates"
---

<objective>
Create the bundled note templates that /vault:init installs into user vaults, and verify the complete init flow works end-to-end.

Purpose: The templates are the tangible artifacts users get from running /vault:init. They define the note structures (frontmatter, sections) that all future commands and agents expect. Without them, SETUP-03 is not met.
Output: Three template files bundled with the vault-init skill, and a verified end-to-end init flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-setup/01-CONTEXT.md
@.planning/phases/01-foundation-setup/01-RESEARCH.md
@.planning/phases/01-foundation-setup/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bundled note templates for vault-init skill</name>
  <files>
    skills/vault-init/templates/project-dashboard.md
    skills/vault-init/templates/task-list.md
    skills/vault-init/templates/inbox-note.md
  </files>
  <read_first>
    .planning/phases/01-foundation-setup/01-RESEARCH.md
    skills/vault-init/SKILL.md
  </read_first>
  <action>
Create directory `skills/vault-init/templates/` and three template files inside it. These are the bundled templates that the /vault:init skill copies into the user's vault `20 Templates/` (or `templates/`) folder during initialization.

Templates use `{{date}}` and `{{title}}` as Obsidian-standard placeholders (compatible with Obsidian core templates and Templater plugin). All frontmatter must be FLAT (no nested objects).

**File 1: skills/vault-init/templates/project-dashboard.md**

YAML frontmatter fields:
- type: project
- status: active
- area: "[[]]"
- tags: []
- created: {{date}}
- updated: {{date}}

Markdown body sections (in order):
- `# {{title}}` -- Title heading
- `## Overview` -- "What is this project and what does done look like?"
- `## Status` -- Bullet list with Phase, Priority, Last updated: {{date}}
- `## Tasks` -- Single unchecked task placeholder `- [ ]`
- `## Key Links` -- Bullet list with wikilink placeholders for Architecture: [[]], Requirements: [[]], Decisions: [[]]
- `## Notes` -- Single dash placeholder
- `## References` -- Single dash placeholder

**File 2: skills/vault-init/templates/task-list.md**

YAML frontmatter fields:
- type: task-list
- project: "[[]]"
- tags: []
- created: {{date}}
- updated: {{date}}

Markdown body sections (in order):
- `# Tasks: {{title}}` -- Title heading
- `## Active` -- Single unchecked task `- [ ]`
- `## Completed` -- Single checked task `- [x]`
- `## Blocked` -- Single blocked task `- [ ] [BLOCKED]`

**File 3: skills/vault-init/templates/inbox-note.md**

YAML frontmatter fields:
- type: inbox
- tags: []
- created: {{date}}

Markdown body:
- `# {{title}}` -- Title heading
- Single dash placeholder line
  </action>
  <verify>
    <automated>test -f /Users/ruslanhalavach/Documents/Projects/vault/skills/vault-init/templates/project-dashboard.md && test -f /Users/ruslanhalavach/Documents/Projects/vault/skills/vault-init/templates/task-list.md && test -f /Users/ruslanhalavach/Documents/Projects/vault/skills/vault-init/templates/inbox-note.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - skills/vault-init/templates/project-dashboard.md contains `type: project` and `status: active` and `## Overview` and `## Tasks` and `## Key Links` and `{{date}}` and `{{title}}`
    - skills/vault-init/templates/task-list.md contains `type: task-list` and `project:` and `## Active` and `## Completed` and `## Blocked` and `{{date}}`
    - skills/vault-init/templates/inbox-note.md contains `type: inbox` and `tags:` and `{{date}}` and `{{title}}`
    - All three templates have YAML frontmatter (delimited by triple-dash lines)
    - Frontmatter is FLAT (no nested objects) -- only scalar values, arrays, and wikilink strings
    - No template contains Handlebars/Mustache logic (no `{{#if}}`, `{{#each}}`) -- only simple `{{date}}` and `{{title}}` placeholders
  </acceptance_criteria>
  <done>Three bundled templates exist in skills/vault-init/templates/ with correct frontmatter, sections, and Obsidian-compatible placeholders</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify plugin loads and /vault:init is functional</name>
  <files>none (verification only)</files>
  <action>
Human verification checkpoint. What was built across Plan 01 and Plan 02:
- plugin.json manifest in .claude-plugin/
- 8 skill directories (vault-init with full body, 5 stubs, 2 reference skills)
- 3 bundled note templates (project dashboard, task list, inbox note)
- SessionStart hook skeleton with lazy dependency install
- package.json with future dependencies

Steps to verify:
1. Run: `claude --plugin-dir /Users/ruslanhalavach/Documents/Projects/vault --debug`
2. Verify in debug output: Plugin "vault" loads, skills register (vault-init visible as slash command)
3. Run `/vault:init --dry-run` in the Claude session
4. Verify: It detects the existing vault structure (00 Inbox/, 01 Projects/, etc.), identifies brownfield state, and presents a proposal WITHOUT executing any changes
5. Verify the proposal includes: missing folders to create, templates to install, vault:config.md generation, CLAUDE.md update, AGENTS.md creation, organization.md migration suggestion
6. Confirm no files were created or modified (dry-run only)

Resume signal: Type "approved" if plugin loads and dry-run works correctly, or describe any issues.
  </action>
  <verify>
    <automated>test -f /Users/ruslanhalavach/Documents/Projects/vault/.claude-plugin/plugin.json && test -d /Users/ruslanhalavach/Documents/Projects/vault/skills/vault-init && test -f /Users/ruslanhalavach/Documents/Projects/vault/skills/vault-init/templates/project-dashboard.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>User has confirmed plugin loads correctly and /vault:init --dry-run produces accurate brownfield proposal</done>
</task>

</tasks>

<verification>
1. All three template files exist in skills/vault-init/templates/
2. Plugin loads in Claude Code with --plugin-dir flag
3. /vault:init --dry-run produces a correct proposal for the existing brownfield vault
4. No files are modified during dry-run verification
</verification>

<success_criteria>
- Three note templates bundled with vault-init skill, each with flat YAML frontmatter and Obsidian-compatible placeholders
- Plugin loads and /vault:init registers as a slash command
- Dry-run of /vault:init on the existing vault correctly detects brownfield state and proposes non-destructive additions
- vault:config.md design includes all four convention types (folder, frontmatter, linking, agent) per D-04
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-setup/01-02-SUMMARY.md`
</output>
